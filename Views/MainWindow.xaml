<Window x:Class="ClaudeSessionManager.Wpf.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:views="clr-namespace:ClaudeSessionManager.Wpf.Views"
        Title="ClaudeSessionManager" Icon="/Assets/app.ico" Width="1280" Height="720"
        Background="#1E1E1E">
    <DockPanel>
        <Border DockPanel.Dock="Top" Background="#2D2D30" BorderBrush="#3F3F46" BorderThickness="0,0,0,1">
            <TabControl x:Name="ModeTabs"
                        SelectionChanged="ModeTabs_SelectionChanged"
                        SelectedIndex="0"
                        Background="Transparent"
                        BorderThickness="0"
                        Padding="0">
                <TabControl.Template>
                    <ControlTemplate TargetType="TabControl">
                        <TabPanel IsItemsHost="True" Background="Transparent" Margin="4,4,4,0"/>
                    </ControlTemplate>
                </TabControl.Template>
                <TabControl.Resources>
                    <Style TargetType="TabItem">
                        <Setter Property="Foreground" Value="#CCCCCC"/>
                        <Setter Property="Padding" Value="14,6"/>
                        <Setter Property="FontSize" Value="13"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="TabItem">
                                    <Border x:Name="Bd"
                                            Background="#2D2D30"
                                            BorderBrush="#3F3F46"
                                            BorderThickness="1,1,1,0"
                                            CornerRadius="3,3,0,0"
                                            Margin="0,0,2,0"
                                            Padding="{TemplateBinding Padding}">
                                        <ContentPresenter ContentSource="Header"
                                                          HorizontalAlignment="Center"
                                                          VerticalAlignment="Center"/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsSelected" Value="True">
                                            <Setter TargetName="Bd" Property="Background" Value="#0E639C"/>
                                            <Setter Property="Foreground" Value="White"/>
                                            <Setter Property="FontWeight" Value="Bold"/>
                                        </Trigger>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="Bd" Property="Background" Value="#3F3F46"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </TabControl.Resources>
                <TabItem Header="1 패널"/>
                <TabItem Header="2 분할"/>
                <TabItem Header="3 분할"/>
                <TabItem Header="4 분할"/>
            </TabControl>
        </Border>
        <views:SessionSidebar DockPanel.Dock="Left" x:Name="Sidebar" MinWidth="180"/>
        <GridSplitter DockPanel.Dock="Left" Width="4"
                      HorizontalAlignment="Stretch" VerticalAlignment="Stretch"
                      Background="#333" ShowsPreview="True"
                      ResizeBehavior="PreviousAndCurrent"/>
        <views:SplitGrid x:Name="SplitArea"/>
    </DockPanel>
</Window>
